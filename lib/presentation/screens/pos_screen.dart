import 'package:flutter/material.dart';

class pos_screen extends StatelessWidget {
  const pos_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('pos_screen')),
      body: const Center(child: Text('Módulo en construcción')),
    );
  }
}
